// Re-export shim -- the palette helpers now live in the cross-codebase toolkit at
// shared/qui, shared with the studio SPA. Import via `$lib/colors` (docs) or
// `$shared/colors` (either build); both land here.
export * from '$shared/colors';
